---
interface Props {
  number: string;
  title: string;
  text: string;
}
const { number, title, text } = Astro.props;
---

<article class="value-card" data-reveal>
  <span>{number}</span>
  <h3>{title}</h3>
  <p>{text}</p>
</article>
